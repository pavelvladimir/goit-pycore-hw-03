[project]
name = "goit-pycore-hw-03"
version = "0.1.0"
description = "Homework repository with separate Python modules for each task."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = []
docs = [
    "sphinx>=8.2.3",
]
